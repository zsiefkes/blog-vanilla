Set database name, username and password in connect-to-db.php and run the following mysql commands to create the tables:

 create table users (
    -> id int(10) unsigned not null auto_increment,
    -> username varchar(30) not null,
    -> password varchar(255) not null,
    -> reg_time timestamp not null default current_timestamp,
    -> enabled tinyint(1) unsigned not null default '1');

alter table users
    -> add primary key (id),
    -> add unique key username (username);

alter table users
     don't need? -> modify id int(10) unsigned not null auto_increment;